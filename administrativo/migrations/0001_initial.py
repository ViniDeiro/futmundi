from django.db import migrations

class Migration(migrations.Migration):

    initial = True

    dependencies = [
    ]

    operations = [
        # Esta é uma migração fake, pois as tabelas já existem no banco
        # Não precisamos criar nada, apenas informar ao Django que está tudo ok
    ] 